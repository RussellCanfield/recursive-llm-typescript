# @rlm/integrations

Optional bridges for LangChain, LangGraph, and DeepAgents.

## What it provides
- Minimal wrapper helpers that call an `RLM` instance

## Usage
- Use `createLangChainRunnable`, `createLangGraphNode`, or `createDeepAgentsTool`.

## Notes
- Peer dependencies are optional; install only the frameworks you use.
